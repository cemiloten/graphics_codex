{
    name = "Make cylinder";

    entities = {
        camera = Camera {
            frame = CFrame::fromXYZYPRDegrees(0, 5, 7, 0, -25, 0);
        };

        skybox = Skybox {
            texture = 0.5;
        };
    };
};
